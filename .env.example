# #HNAG M0/M1
# The core random/blindbox game requires NO environment variables.
# It must run with only the repository, pnpm install, and pnpm dev/build.
#
# Add future VITE_* public flags only when the corresponding feature is actually implemented.
# Never place secrets in VITE_* variables because Vite exposes them to the browser.
